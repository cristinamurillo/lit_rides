

      <div class="centered">
     
        <h1 class="centered title">GET A LIT RIDE</h1>
         <%= image_tag("steering_wheel.png", class: "wheel-icon")  %>
        <% if session[:user_id]==nil%>
         
        <h1 class = "centered sign-up"><%= button_to "SIGN UP", new_user_path, method: :get, class: "gen-btn"%></h1>
        <% end%>
      </div>
    


<%# if session[:user_id] == nil %>
  <%# link_to "Log In", login_path %>
  <%#link_to "Sign Up", new_user_path %>
<%#else %>
  <%# "You are already logged in!" %>
  <%# link_to "Continue to Dashboard", "users/#{session[:user_id]}/main_page" %>
  <%# link_to "Browse Rides", rides_path %>
  <%#link_to "Log Out", logout_path, method: :delete %>
<%# end %>
