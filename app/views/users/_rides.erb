<ul>
  <%= button_to ride_path(ride) do %>
  <div>
    <%= "Departure: #{ride.departure}"%> <br>
    <%= "Destination: #{ride.destination}" %> <br>
    <%= "Date: #{ride.time.strftime('%B %e')}" %> <br>
  </div>
  <% end %>
</ul>
